"use client";

export default function PendingCoursesPage() {
  // This will fetch and display pending courses for admin approval
  // You can add fetch logic and approval button as needed
  return (
    <div className="min-h-screen bg-gray-100 p-8">
      <h1 className="text-2xl font-bold mb-6">Pending Courses for Approval</h1>
      <p>List of courses pending admin approval will appear here.</p>
    </div>
  );
}
